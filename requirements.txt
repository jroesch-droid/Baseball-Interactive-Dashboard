pybaseball>=2.2.7
pandas>=2.0
numpy>=1.24
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
# Optional: only needed for the tier-3 FanGraphs browser fallback.
# If installed, the workflow must also run: playwright install chromium
playwright>=1.44
